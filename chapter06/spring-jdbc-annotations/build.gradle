dependencies {
	compile db.mysql, db.dbcp
}

jar {
	manifest {
		attributes(
				'Main-Class': 'com.apress.prospring5.ch6.AnnotationJdbcDaoSample',
				"Class-Path": configurations.compile.collect { it.getName() }.join(' '))
	}
}

